# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    # Database configuration
    database_server_version: '18'

    # OpenTelemetry frontend configuration
    otel.frontend.enabled: '%env(bool:OTEL_FRONTEND_ENABLED)%'
    otel.frontend.api_key: '%env(string:OTEL_FRONTEND_API_KEY)%'
    otel.frontend.endpoint: '%env(string:OTEL_FRONTEND_ENDPOINT)%'
    otel.frontend.service_name: '%env(string:OTEL_FRONTEND_SERVICE_NAME)%'
    otel.frontend.service_version: '%env(string:OTEL_FRONTEND_SERVICE_VERSION)%'

    # Storage configuration (local or S3/MinIO)
    # Default values for env var fallback
    storage.type.default: 'local'
    storage.s3.region.default: 'us-east-1'
    storage.s3.use_path_style.default: 'false'

    storage.type: '%env(default:storage.type.default:STORAGE_TYPE)%'
    storage.local_path: '%kernel.project_dir%/storage/parquet'
    storage.s3.bucket: '%env(S3_BUCKET)%'
    storage.s3.region: '%env(default:storage.s3.region.default:S3_REGION)%'
    storage.s3.endpoint: '%env(S3_ENDPOINT)%'
    storage.s3.access_key: '%env(S3_ACCESS_KEY)%'
    storage.s3.secret_key: '%env(S3_SECRET_KEY)%'
    storage.s3.use_path_style: '%env(bool:default:storage.s3.use_path_style.default:S3_USE_PATH_STYLE)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    # OpenTelemetry frontend Twig extension
    App\Twig\OtelExtension:
        arguments:
            $otelEnabled: '%otel.frontend.enabled%'
            $otelApiKey: '%otel.frontend.api_key%'
            $otelEndpoint: '%otel.frontend.endpoint%'
            $otelServiceName: '%otel.frontend.service_name%'
            $otelServiceVersion: '%otel.frontend.service_version%'
        tags: ['twig.extension']

    # Storage factory for Parquet files (supports local and S3/MinIO)
    App\Service\Storage\StorageFactory:
        arguments:
            $storageType: '%storage.type%'
            $localPath: '%storage.local_path%'
            $s3Bucket: '%storage.s3.bucket%'
            $s3Region: '%storage.s3.region%'
            $s3Endpoint: '%storage.s3.endpoint%'
            $s3AccessKey: '%storage.s3.access_key%'
            $s3SecretKey: '%storage.s3.secret_key%'
            $s3UsePathStyle: '%storage.s3.use_path_style%'

    # Async query result store with dedicated cache pool
    App\Service\QueryBuilder\AsyncQueryResultStore:
        arguments:
            $cache: '@async_query.cache'

    # Parquet reader service with performance logger
    App\Service\Parquet\ParquetReaderService:
        arguments:
            $logger: '@monolog.logger.performance'
