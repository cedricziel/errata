{
  "name": "errata",
  "scripts": {
    "dokku": {
      "predeploy": "php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration"
    }
  },
  "healthchecks": {
    "web": [
      {
        "type": "startup",
        "name": "web check",
        "path": "/api/v1/health",
        "attempts": 10,
        "wait": 5
      }
    ]
  }
}
